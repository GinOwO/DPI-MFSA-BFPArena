# BpfObject.cmake - Helper module for BPF object compilation
#
# This module is currently a placeholder. BPF compilation is handled
# directly in src/dataplane/CMakeLists.txt via custom commands.
#
# Future: could provide reusable functions like:
#   bpf_object(NAME <name> SOURCE <src> [DEPENDS ...])
